// import mongoose from "mongoose";



// const MoodSchema = new mongoose.Schema({
//   mood_name: { type: String, required: true }, // E.g., "Happy", "Relaxed"
//   mood_image: { type: String, required: true }, // URL of an image representing the mood
//   recommended_items: [
//     {
//       item_name: { type: String, required: true }, // E.g., "Ice Cream"
//       item_image: { type: String, required: true }, // URL of the item's image
//       item_id: { type: mongoose.Schema.Types.ObjectId, ref: 'food' }, // Reference to related menu items (if any)
//     },
//   ],
// });


// const MoodModel = mongoose.model('Mood', MoodSchema);

// export default MoodModel;  // Export as default

